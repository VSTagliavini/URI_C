#include <stdio.h>
#include <string.h>

int main ()
{
    int aux;
    char carta[101];
    while (1)
    {
        scanf(" %[^\n]s", &carta);
        if (strcmp(carta, "0")==0) break;
        for (aux=0; aux<strlen(carta); aux++)
        {

        }
    }
}